package com.posn.clouds;

/**
 * This interface class defines a listener that is invoked when the application is connected the a cloud provider
 **/
public interface OnConnectedCloudListener
   {
      void OnConnected();
   }
